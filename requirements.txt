# ============================================================
# AI Bank Statement Automation - Pinned Requirements
# Generated: June 2026 (for reproducibility & conflict isolation)
# ============================================================
# IMPORTANT:
# - All critical packages are pinned to exact versions
# - This makes it easier to reproduce bugs and isolate conflicts
# - To debug conflicts, run: pip install pipdeptree && pipdeptree
# - Recommended future upgrade path: pyproject.toml + uv or pip-tools
# ============================================================

# --- Core / UI ---
streamlit==1.45.0
python-dotenv==1.2.2  # 1.0.1
PyYAML==6.0.2

# --- LLM Providers ---
#openai==2.35.0
instructor==1.15.1
google-generativeai
langchain-google-genai
langchain-ollama==0.3.3          # pinned - works well with current Ollama
litellm==1.79.2   #>=1.79.2  #1.73.6                  # pinned - unified LLM calls

# --- RAG & Vector Stores ---
langchain==0.3.25
langchain-community==0.3.24
langchain-text-splitters==0.3.8
langchain-chroma==0.2.6
langchain-huggingface==0.2.0
chromadb>=1.0.20
faiss-cpu==1.10.0
fastembed==0.6.1
fastembed-gpu==0.6.1

# --- Document Processing ---
pytesseract==0.3.13
opencv-python==4.11.0.86
PyPDF2==3.0.1
pymupdf==1.26.6 #1.25.5
pymupdf4llm==0.0.17
langchain-docling==1.0.0
ultralytics==8.4.67   # 8.3.78
# ultralyticsplus==0.1.0

# --- Data Analysis & Visualization ---
pandas==2.2.3
numpy==2.2.5
scipy==1.15.2
scikit-learn==1.6.1
matplotlib==3.10.3
seaborn==0.13.2
openpyxl==3.1.5
plotly==6.0.1
plotly-express==0.4.1
httpx==0.28.1

# --- AI Agents (CrewAI + LangGraph) ---
# Pinned to modern stable version that supports CrewAI Skills feature
crewai==1.14.7
crewai[tools]==1.14.7
langgraph==0.4.8

# --- Evaluation & Observability ---
# deepeval==2.0.0 #2.9.4
ragas==0.2.14
opik==2.0.64   #1.7.40
# langchain-google-vertexai==2.0.0

# --- LLM Inference Optimization (Local) ---
# vllm==0.8.5
optimum==1.24.0
accelerate==1.6.0
bitsandbytes==0.45.5
sentence-transformers==4.1.0
datasets==3.6.0
huggingface_hub==0.31.2
mlflow==3.13.0


# --- Compatibility Pins ---
rich #==13.9.4
textual #==0.85.1

# ============================================================
# Notes for Conflict Isolation
# ============================================================
# If you get dependency conflicts:
# 1. pip install pipdeptree
# 2. pipdeptree --warn silence | grep -i conflict
# 3. Or use: pip install "package==x.y.z" --no-deps   (to test one package)
#
# To upgrade safely later:
# - Change one package at a time
# - Re-test with your notebooks + Streamlit app
# - Consider switching to pyproject.toml + uv for automatic resolution

